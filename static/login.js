document.getElementById('loginForm').addEventListener('submit', function(e){
  const errorDiv = document.getElementById('error');
  // Let browser handle form submission normally since it's server handled - no AJAX needed
});
